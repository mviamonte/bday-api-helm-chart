apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service 
  labels:
    app: {{ .Values.applicationName }}
  namespace: app-space
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.applicationName }}
  ports:
    - protocol: TCP
      name:  {{ .Values.ingress.name }}
      port: {{ .Values.ingress.port }}
      targetPort: {{ .Values.connectivity.applicationPort }}